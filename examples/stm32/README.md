# STM32 Example

Compile-verified skeleton for STM32 flash integration.

Use the STM32 HAL header in `backends/hal_examples/` to connect the real flash region and program unit for your target family.

